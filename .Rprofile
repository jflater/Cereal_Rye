# Load the here package for relative paths
if (!requireNamespace("here", quietly = TRUE)) {
  install.packages("here")
}
library(here)

# Default message on project load
message("Welcome to the Cereal_Rye project! Using relative paths with 'here'.")

# Set options (e.g., display preferences)
options(stringsAsFactors = FALSE)

# Auto-load frequently used libraries
if (!requireNamespace("tidyverse", quietly = TRUE)) {
  install.packages("tidyverse")
}
library(tidyverse)

# Set working directory to project root
setwd(here::here())
